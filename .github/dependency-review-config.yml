# Dependency Review (só em pull request): bloqueia dependência NOVA com vulnerabilidade alta ou
# crítica que vá para produção. Opções válidas: github.com/actions/dependency-review-action
fail-on-severity: high
fail-on-scopes:
  - runtime
# allow-ghsas:
#   - GHSA-xxxx-xxxx-xxxx
